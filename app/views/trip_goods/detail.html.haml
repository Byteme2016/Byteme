.container-fluid
  .container
    .row.col-sm-offset-1
      #p.h1 #{@trip_good.title}

    .row.col-sm-offset-1
      #p.h3 #{@trip_good.introduction}

    .row.col-sm-offset-9
      =link_to "Order Now!", ordered_trips_path(:id=>@trip_good.id), :method=>'post', :confirm=>"Are you sure to order this trip?", :class=>"btn btn-default"
      =link_to "Go Back", '/search_goods', :method=>'post', :class=>"btn btn-default"