  #order.container
  - if @orders== nil || @orders.size == 0
    %h2 No result
  - else
    .container
      .row
        - @orders.each do |e|
          .col-sm-6.col-md-4
            .thumbnail
              = link_to "#{e.title}" new_ordered_trip_path
              %span #{e.price}
              %p #{e.introduction}